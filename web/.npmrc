# The production bundle is produced by devDependencies (typescript, vite,
# @vitejs/plugin-react), so `npm run build` needs them installed even when the
# build runs with NODE_ENV=production. Without this, npm omits devDependencies
# and the build dies with "sh: tsc: command not found".
include=dev
